{
  "name": "@wellpulse/database",
  "version": "0.0.1",
  "private": true,
  "description": "Shared database types and utilities for WellPulse monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.3.0"
  }
}
