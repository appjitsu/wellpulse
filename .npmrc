# Prevent npm usage (enforce pnpm)
engine-strict=true

# Fix TypeScript type resolution issues with pnpm (TS2742)
# Use hoisted node-linker for better TypeScript compatibility
node-linker=hoisted

# Use pnpm's strict peer dependencies
strict-peer-dependencies=false

# Audit level
audit-level=moderate
